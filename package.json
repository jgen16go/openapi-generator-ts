{
  "name": "oepnapi_generator_test",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "gen-api-client": "openapi-generator generate --skip-validate-spec -i doc/swagger.json -g typescript-axios -c ./config/config.json -o ./lib",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@openapitools/openapi-generator-cli": "^1.0.13-4.3.1"
  },
  "dependencies": {
    "axios": "^0.21.1"
  }
}
